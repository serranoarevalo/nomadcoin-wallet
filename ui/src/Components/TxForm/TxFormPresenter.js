import React from "react";
import PropTypes from "prop-types";
import { Card, Key, KeyName } from "Components/Shared";

const TxFormPresenter = () => (
  <Card>
    <Key>Send NMD: </Key>
  </Card>
);

export default TxFormPresenter;
